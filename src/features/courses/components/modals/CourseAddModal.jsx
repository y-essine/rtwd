import { Dialog } from "@headlessui/react";
import Modal from "@/shared/components/ui/modals/Modal";

const CourseAddModal = ({ element }) => {
    return <Modal element={element}></Modal>;
};

export default CourseAddModal;
